import { createTable, iniciar } from './main';

// describe('Prueba de function createTable', () => {
//     test('Recibiendo un array  y debe cambiar el contenido, tener al menos un 1', () => {
//         const arr = [
//             [0, 0, 0, 0, 0],
//             [2, 2, 2, 2, 2],
//         ];

//         expect(iniciar(arr)).toContain(1);
//     });
// });
